---
name: competitor-matrix
title: 竞品对比稿
description: 适合多实体题面，如「X 与 Y 的对比」「X 竞品分析」；实体×维度矩阵开篇，逐维度展开，收尾一张「谁强在哪」总表加一节「这意味着什么」。
tables: [entity_dimension, entity_mentions, attitude_by_topic, quotes, scenario_attitude, audience_attitude, trigger_counts, platform_mix, grade_mix, crossref_mix]
sections: [执行摘要, 对比总览, 逐维度对比, 谁强在哪, 这意味着什么, 建议, 附录]
model: opus
---

# 竞品对比稿

读者要在几个产品之间做判断。他们要的是**一张能横着读的表**，加上表里每一格的依据。
一级标题就用 `sections` 里那六个词。

维度沿用竞品画像章的结尾字段：**产品定位 / 能力侧重 / 国内用户口碑 / 价格与门槛 / 渠道与生态**。
`entity_dimension` 表里出现了别的维度就一并纳入，没证据的维度整列去掉，不许留空列充数。

## 执行摘要

SCQA 一段（≤150 字），答案那句必须是**一个可比较的判断**：谁在什么维度上占优、
差距有多大、这个判断的证据有多硬。

然后 3–5 条关键发现，格式同咨询体：`1. 【A】结论句 [S12]`。
对比稿的发现要落在**实体之间的差异**上，不是某个实体自己的特点。

最后固定一句人话置信度提示（共用规则 6.5.2）：

> 本报告结论的把握度为**低 / 中 / 高**，主要因为……

**那个档位不是你定的**：程序按**每条关键发现**自己引的证据算（几个独立出处 / 几个平台 /
最强等级 / 落在编码表哪一格有多少条），整篇取各条的中位档。你那句话里的档位必须写
程序算出来的那一个，对不上当轮打回；「主要因为……」那半句写**哪几条强、哪几条弱**
的人话理由。⛔ 不许写「多数说法只有一个来源所以低」——那句在本项目语料上几乎恒真，
等于每份报告开篇都劝读者别信（共用规则 §6.5.2 里有真稿例句，照那个形状写）。

摘要正文里不许出现 SINGLE / PASS / WEAK / CONFLICT 这些内部口径词，也不许写「253/274」这类方法论读数——那些留给附录。

**把握度判「低」的那几条发现，标题不得大过证据**（共用规则 §6.5.2′，程序当轮核）：
标题里不许出现「普遍 / 大多数 / 主流 / 集体 / 广泛」这类普遍化断言词，
发现行里要交代证据规模或范围（「两篇公众号文章」「同一个帖子下的两条评论」
「本轮样本里」）。判「高」的那几条照旧正常写，不必加限定。

## 对比总览

**一张实体 × 维度矩阵**，直接用 `entity_dimension` 表。**格里只写证据条数，不写角标**——
每格塞一串角标，这张表就横着读不了了（实测每格 9–13 个）。角标归到表下面那一行：
`n= · 口径 · 来源角标`，一行最多列 3 个，完整清单在文末信息源清单里。

矩阵下面写三到五句：哪些格子密（说明这个维度上讨论多）、哪些格子空（说明没采到，
不等于这个产品在这个维度上差）。**空格子只能解读成「本轮没采到」，不能解读成「不行」**——
这是对比稿最容易出的错。

同时看 `entity_mentions` 表：提及量差距大时，要点明这可能是取数偏差而非真实热度差
（平台分布表自己去看，别把它搬进正文）。这句限定只写一句话，不配表、不写「被引」
「采集条数」「采集量」这些取数口径词——对比总览、逐维度对比、谁强在哪这三节
只讲对象，取数过程归附录（见共用规则 §4.5）。

## 逐维度对比

每个维度一节，二级标题是**一句带比较的结论**（行动式标题），例如
「在回答质量上，A 的抱怨集中在准确性、B 集中在回避提问」。每节：

1. 该维度下各实体的说法，一实体一小段，每段至少一个角标。
2. **一句横向对比**：这个维度上谁更强、依据是什么。
3. 证据不够就直说：「本维度 A 只有 2 条 C 级证据，不足以与 B 比较」。
   **限定一句话说完**，不要另起「假设与不确定性」小节——那一节全篇只在附录写一次（共用规则 §5.1）。

不许在任何一个维度上给没有证据的实体编说法。

## 谁强在哪

**一张总表**，一行一个实体：

| 实体 | 强在哪 | 弱在哪 | 证据强度 | 角标 |
|---|---|---|---|---|

「证据强度」写这个实体名下被引证据的最高等级与条数（查 `grade_mix` 与 `entity_mentions`）。
证据强度是 C 或只有单源的，「强在哪 / 弱在哪」两栏必须带「（单源，待核实）」字样。

表下面用一句话回答读者真正的问题：**如果只能选一个，什么场景选谁。** 如果证据不足以回答，
就明说不足以回答，不要含糊过去。

## 这意味着什么

上一节回答的是「谁强在哪」，这一节回答**「所以这对你意味着什么」**——问这个问题的人
不是来看热闹的，他手上有个决定要做。给你的输入里有一段「读者是谁」，先看那个。

读者身份明确时，整节写给那一位，一段三到五句：他所在的位置在这张对比表里处在哪，
哪个维度上的差距对他是机会、哪个是威胁，最要紧的一件事是什么。每句带角标。

读者身份是「不明」时，这一节固定分三行，一行一类读者，各写一句含义：

```
- **对竞品团队**：这张表对你意味着什么[S12]
- **对本产品团队**：这张表对你意味着什么[S15]
- **对投资与分析**：这张表对你意味着什么[S21]
```

三条硬规矩：

1. **只写含义，不写动作**——动作归下一节「建议」，这里写完读者该明白自己的处境。
2. **不许替某一方站队**：这份报告的证据来自公开 UGC 与媒体，不是谁的内部数据，
   写「对本产品团队意味着口碑在内容生产链路上最集中」可以，写「你们应该收购它」不行。
3. 「弱证据不撑强建议」的门禁照旧管这几行：只有单源孤证撑着的含义，
   写成「需要先验证什么」，别写成断言。真要降级，形态按共用规则 §6.5.4
   「降级区写成什么样」照做——程序只认独立小标题和条目号之后紧挨着的起始前缀两种。

## 建议

同咨询体：按「影响 × 把握度」排序，每条回指某条发现，弱证据不撑强建议。
两道闸分工见共用规则 §6.5.4：一道看交叉验证结论，一道看证据强度；第二道命中时
要么降级，要么把 `把握度：低（理由）` 写进依据行。
对比稿的建议要落在**选择或应对**上（选哪个、防哪个、补哪块），不是泛泛的「持续关注」。

降级的那几条排在这一节最后，形态同样按共用规则 §6.5.4「降级区写成什么样」照做，
写在句子中间不算降级、会被原样打回。

## 附录

「假设与不确定性」写在这一节，全篇只此一处（共用规则 §5.1）。
按共用规则第 7 条五件事写。对比稿额外交底一件：**各实体的采集量是否均衡**，
不均衡就写明白，免得读者把采集偏差当成市场事实。

**代表原声表由程序接在这一节，你一个字都不要写。** 但它的数据仍然投给你——
正文里的原声**引用块**照共用规则 §5.6 步骤 4 写。
⚠️ 某一格没有可引的原话就是真的没有（表已按等级筛过，A/B/C，D 不进），照 §5.6 那条分情况诚实写：
真没有原话才说没有；有原话、等级是 C 的照引并标「等级 C」，⛔ 不许写「未截取到评论正文」。
⛔ 不许去够 D 级证据或帖子标题凑——两样都会被程序闸原样打回。
⛔ 只是**整张表**不许摆进正文（同一张表出现两次，读者第二次只会跳过）。
